describe('randomItemSpinner', function() {
    it('Should randomize an array', function() {
        expect(false).toEqual(true);
        var React = require('react/addons');
    });
    it('Should randomize an array', function() {
        expect(false).toEqual(true);
    });
});
